{% extends 'base.html.twig' %}

{% block title %}Fuse | nouvelle ressource{% endblock %}

{% block body %}
    <div class="container mt-5 text-center">
        <h1>Nouvelle ressource</h1>
        {{ form_start(form_ress) }}

        <br>
        <br>

        <div class="row flex">
            <div class="col-md-4 text-center">{{ form_row(form_ress.category) }}</div>
{#            {{ dump(form_ress) }}#}

            {% if category != null %}
                <div class="col-md-4 text-center">
                {% for categ_attr in category.categoryAttributes %}
                    {{ form_row(attribute(form_ress, 'ressource_attribute' ~loop.index0 )) }}
                {% endfor %}
                </div>
                <div class="col-md-4 text-center">{{ form_row(form_ress.description) }}</div>
            {% endif %}

        </div>
        <br>
        <br>
        {% if category != null %}
        <div class="row">{{ form_row(form_ress.ressourcePicture) }}</div>
        {% endif %}

        <br>
        <br>

        <input type="submit" name="submit_button" value="BOUM" class="btn btn-dark" />


        {{ form_end(form_ress) }}



        {#        ca est équivalent à l'affichage brut#}

{#        {{ form_start(form) }}#}
{#        <div class="col-md-4">#}
{#            {{ form_row(form.name) }}#}
{#        </div>#}
{#        <div class="col-md-4">#}
{#            {{ form_row(form.email) }}#}
{#        </div>#}
{#        <div class="col-md-4">#}
{#            {{ form_row(form.password) }}#}
{#        </div>#}

{#        {{ form_rest(form) }}#}
{#                form_widget c'est pour afficher le formulaire avec en parametre le formulaire#}
{#        <button class="btn-primary mt-4">Boudoum</button>#}
{#        {{ form_end(form) }}#}


    </div>

{#    <div class="container mt-5 text-center">#}
{#        <h1>Nouvelle ressource2</h1>#}
{#        {{ form_start(form_attr) }}#}

{#        <input type="submit" name="submit_button" value="BOUMDOUM" class="btn btn-dark" />#}

{#        {{ form_end(form_attr) }}#}
{#    </div>#}

{% endblock %}
